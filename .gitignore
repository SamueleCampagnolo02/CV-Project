# Virtual environment
taco-env/

# Raw datasets (regenerable via the scripts in taco_prep/)
TACO/
taco_yolo/
taco_yolo_macro/
custom_syringe_yolo/
custom_syringe/images/
custom_syringe/test_images_2/

# Training run outputs (plots, logs, training batches: heavy and regenerable by retraining)
runs/
runs_taco/
runs_taco_macro/
runs_syringe/

# Transfer archives (Colab/Kaggle <-> local)
*.zip

# C++ build artifacts (regenerable via cmake, sources stay tracked)
inference/build/
inference_syringe/build/

# Python cache
__pycache__/
*.pyc

# Personal/exploratory scripts, not part of the tracked project
other/
# Local PyTorch checkpoints for incremental training (not versioned, regenerable)
*.ptsyringe-detection/time-evaluation/build/
